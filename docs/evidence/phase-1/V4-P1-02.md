# Evidence V4-P1-02 — Auth configuration fail-closed

- Date: 2026-08-21
- Commit: `c1ca5a1`
- Requirement: synthetic mode is allowed only when all Auth variables are blank;
  partial configuration must not silently expose the app.

## Verification

- Command: `npm run test`
- Expected: complete, partial and blank runtime configuration cases are deterministic.
- Actual: 125 Vitest tests passed, including `src/auth/runtimeConfig.test.ts`.
- Result: PASS.
- Artifact: `src/auth/runtimeConfig.ts` and `src/main.tsx`.

## Privacy review

- The invalid-config UI exposes only a stable generic message; it does not
  display URLs, keys or environment values.
