# Evidence V4-P2-08 — Scheduled read-only dispatch

- Date: 2026-08-21
- Commit: `c1ca5a1`
- Requirement: scheduled UTC jobs use only `sync_teaching` or
  `read_lms_pending`, an empty payload and deterministic date/time-window
  idempotency keys.

## Verification

- Command: `npm exec vitest run test/cron-workflow.test.ts`
- Expected: request shape, missing configuration, UUID/status validation and
  all three schedules pass.
- Actual: 6 focused tests passed; the full Vitest suite was 125/125.
- Result: PASS.
- Artifacts: `scripts/cron_dispatch.mjs` and `.github/workflows/cron-dispatch.yml`.

## Privacy review

- Headers are never printed; request body contains no credentials or page data.
- The workflow has `contents: read` and no mutation type such as Save/Submit.
