# Evidence V4-P2-07 — CLI heartbeat and cleanup

- Date: 2026-08-21
- Commit: `c1ca5a1`
- Requirement: long-running adapters refresh the lease, terminate at the hard
  twelve-minute application timeout, and browser cleanup remains in `finally`.

## Verification

- Command: `uv run pytest apps/browser-runner/tests/unit/test_cli.py -q`
- Expected: a long synthetic adapter receives a heartbeat, finishes safely and
  closes the browser session.
- Actual: CLI unit tests passed; full Python suite was 217/217.
- Result: PASS.
- Artifact: `apps/browser-runner/src/mindx_runner/cli.py`.
- The twelve-minute execution deadline starts before claim/browser startup;
  synchronous RPCs are awaited through bounded worker calls, and browser
  cleanup has a one-second bound.

## Privacy review

- The adapter is synthetic and read-only; no browser recording, screenshot,
  cookie or page content is stored.
