# Evidence V4-S0-03 — Edge dispatch probe

- Date: 2026-08-11
- Commit: `80aff44..974b81b`
- Environment: synthetic-only Vitest mocks and workflow contract
- Data class: synthetic
- Requirement: An authenticated Edge Function can dispatch one GitHub workflow.

## Command/steps

1. Run `npx vitest run test/dispatch.test.ts test/edge-dispatch.test.ts test/workflow-contract.test.ts --reporter=verbose`.
2. Run `npm run verify:no-secrets` and `npm run verify:no-live-write`.
3. Inspect the Deno entrypoint and synthetic GitHub workflow for server-only
   configuration, read-only permissions, strict inputs and no LMS mutation.

## Expected

Manual requests require an authenticated workspace owner, Cron requests require
the configured secret and workspace, and the Edge path dispatches only an
allowlisted synthetic job type without returning or logging credentials.

## Actual

- The focused suite passed: 3 test files and 16 tests.
- Mocked manual JWT and Cron paths reached the RPC and GitHub adapters.
- Mocked GitHub success returned `202`; mocked GitHub failure marked the job
  `dispatch_failed` and returned the safe error code.
- The workflow contract passed UUID/job-type validation, read-only permissions,
  per-job concurrency and the no-Save/Submit assertion.
- The secret and live-write scanners passed.
- No real Edge deployment, GitHub request or billed workflow run was performed;
  owner-controlled secrets and cloud infrastructure are not configured here.

## Result

- BLOCKED — synthetic implementation and tests PASS; live/cloud dispatch remains
  BLOCKED.

## Artifacts

- `supabase/functions/dispatch-job/index.ts`
- `supabase/functions/_shared/dispatch.ts`
- `supabase/functions/_shared/edgeAdapters.ts`
- `supabase/functions/_shared/http.ts`
- `.github/workflows/spike0-dispatch-probe.yml`
- `test/dispatch.test.ts`
- `test/edge-dispatch.test.ts`
- `test/workflow-contract.test.ts`

## Privacy review

- [x] No credentials
- [x] No cookies/tokens
- [x] No student names/notes
- [x] No raw page screenshot
