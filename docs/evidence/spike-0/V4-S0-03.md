# Evidence V4-S0-03 — Edge dispatch probe

- Date: 2026-08-12
- Commit: `fd4c52a92c97bdb690f0b0865ca6372791183097`
- Environment: hosted Supabase Edge Function and GitHub Actions
- Data class: synthetic
- Requirement: An authenticated Edge Function can dispatch one GitHub workflow.

## Command/steps

1. Deploy `dispatch-job` to the hosted Supabase project with server-side
   configuration and custom Cron authentication enabled.
2. Send one synthetic `sync_teaching` request to the hosted function using the
   Cron secret entered privately by the owner. The request body contained an
   existing idempotency key and an empty payload.
3. Confirm the HTTP response and inspect the owner-controlled GitHub Actions
   run for `.github/workflows/spike0-dispatch-probe.yml`.

## Expected

The authenticated request returns `202` with `status: "dispatched"`, and the
GitHub workflow accepts only synthetic UUID/job-type inputs with read-only
permissions and no LMS mutation.

## Actual

- Supabase returned `HTTP 202` with `status: "dispatched"` and `created: false`.
  `created: false` records the retry of the existing idempotency key after the
  workflow was published to the `main` branch.
- The returned synthetic job ID was `7357a2f0-251c-4e08-9700-126b8c71e33e`.
- GitHub Actions run `spike0-dispatch-probe #1` ran on `main` at commit
  `fd4c52a`, completed with `Success`, and finished in 9 seconds. Its `validate`
  job completed successfully.
- The workflow input was synthetic; no Teaching or LMS page was opened and no
  LMS Save/Submit action was available.
- No credential, token, cookie or real student data was recorded in this
  evidence.

## Result

- PASS — hosted Supabase Edge dispatch reached and completed the synthetic
  GitHub Actions workflow.

## Artifacts

- `supabase/functions/dispatch-job/index.ts`
- `supabase/functions/_shared/environment.ts`
- `supabase/functions/_shared/dispatch.ts`
- `supabase/functions/_shared/edgeAdapters.ts`
- `supabase/functions/_shared/http.ts`
- `supabase/config.toml`
- `.github/workflows/spike0-dispatch-probe.yml`
- `test/dispatch.test.ts`
- `test/edge-dispatch.test.ts`
- `test/edge-environment.test.ts`
- `test/supabase-config.test.ts`
- Owner-observed GitHub Actions run `spike0-dispatch-probe #1`

## Privacy review

- [x] No credentials
- [x] No cookies/tokens
- [x] No student names/notes
- [x] No raw page screenshot committed
- [x] Synthetic workflow only; no LMS mutation
