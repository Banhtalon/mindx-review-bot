# Evidence V4-S0-04 — Job idempotency and atomic claim

- Date: 2026-08-11
- Commit: `80aff44..974b81b`
- Environment: synthetic TypeScript state machine and pgTAP migration/test
- Data class: synthetic
- Requirement: Duplicate dispatches do not create duplicate jobs.

## Command/steps

1. Run the focused dispatch/adapter/workflow suite; it passed 16/16 tests.
2. Run `npm test`; it passed 9 files and 39 tests.
3. Run `npm run test:rls`; the command attempted the local Supabase database
   and failed before test execution because Docker's Postgres endpoint was not
   listening on `127.0.0.1:54322`.

## Expected

The same workspace/idempotency key is accepted once and returns the existing
job on reuse, a different type/payload conflicts, and only one caller can move
the job into `dispatching` before the terminal dispatch transition.

## Actual

- The migration adds a unique `(workspace_id, idempotency_key)` constraint,
  service-role-only RPCs, same-key comparison, atomic claim and terminal
  `dispatched`/`dispatch_failed` transitions.
- The pure orchestration tests cover first dispatch, same-key reuse, payload
  conflict, claim contention and GitHub failure without a second GitHub call.
- The pgTAP file contains 16 assertions for RPC behavior and authenticated
  client rejection, but it was not executed because the Docker daemon is not
  running in this environment.

## Result

- BLOCKED — synthetic implementation/tests PASS; database execution is BLOCKED
  until Docker/Supabase is available.

## Artifacts

- `supabase/migrations/20260810000001_spike0_dispatch.sql`
- `supabase/tests/0002_spike0_dispatch.sql`
- `supabase/functions/_shared/dispatch.ts`
- `test/dispatch.test.ts`
- `test/edge-dispatch.test.ts`

## Privacy review

- [x] No credentials
- [x] No cookies/tokens
- [x] No student names/notes
- [x] No raw page screenshot
